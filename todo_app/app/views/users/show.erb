<h1><%= @user.username %></h1>

<% @lists.each do |list| %>
  <div class='list'>
    <%= list.name %><br>
    <%= link_to "Edit", edit_list_path(list) %>
    <%= link_to "Delete", list, method: :delete %>
  </div>
<% end %>

<%= link_to "Home", :root %>
<%= link_to "Logout", logout_path %>
